---
/* R9 i18n — English /en/404 route (real page, never translate.goog). Same
   body as the Hebrew page — see components/NotFoundPage.astro. */
import Layout from '../../layouts/Layout.astro';
import NotFoundPage from '../../components/NotFoundPage.astro';
import { notfound } from '../../i18n/pages/notfound';

const t = notfound.en;
---

<Layout title={t.pageTitle} description={t.pageDescription} current="home" navMode="static" locale="en">
  <NotFoundPage locale="en" />
</Layout>
