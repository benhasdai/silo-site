---
/* R9 i18n — Russian /ru/giftcard route (real page, never translate.goog).
   Same body as the Hebrew page — see components/GiftcardPage.astro. */
import Layout from '../../layouts/Layout.astro';
import GiftcardPage from '../../components/GiftcardPage.astro';
import { giftcard } from '../../i18n/pages/giftcard';

const t = giftcard.ru;
---

<Layout title={t.pageTitle} description={t.pageDescription} current="giftcard" navMode="static" locale="ru">
  <GiftcardPage locale="ru" />
</Layout>
